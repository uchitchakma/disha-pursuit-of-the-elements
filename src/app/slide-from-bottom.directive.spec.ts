import { SlideFromBottomDirective } from './slide-from-bottom.directive';

describe('SlideFromBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideFromBottomDirective();
    expect(directive).toBeTruthy();
  });
});
